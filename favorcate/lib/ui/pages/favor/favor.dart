import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class JYFavorPage extends StatelessWidget {
  static const String routeName = "/favorPage";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("收藏"),centerTitle:true),
      body: Container(),
    );
  }
}
